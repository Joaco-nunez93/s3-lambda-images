{
  "name": "s3-lambda-images",
  "version": "0.1.0",
  "bin": {
    "s3-lambda-images": "bin/s3-lambda-images.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.1033.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.230.0",
    "constructs": "^10.4.3"
  }
}
